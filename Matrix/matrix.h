int* new_matrix(int rows, int cols) 
{
    int* matrix = (int*) malloc(rows * cols * sizeof(int));
    return matrix;
}


// perform matrix multiplication between two multi dimensional arrays
int* mat_mul(int* m1, int* m2) 
{
    
}
